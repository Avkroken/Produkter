name: Trivy
description: >-
  Skannar en redan byggd, lokalt laddad image, rapporterar alla relevanta fynd
  till Code Scanning och gör fixerbara HIGH/CRITICAL-fynd blockerande via
  anropande jobb.

inputs:
  image-ref:
    description: Fullständig tagg på den lokalt laddade imagen som ska skannas.
    required: true
  category:
    description: Unik Code Scanning-kategori för imagen.
    required: true

runs:
  using: composite
  steps:
    - name: Skanna imagen för rapportering
      uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
      with:
        scan-type: image
        image-ref: ${{ inputs.image-ref }}
        format: sarif
        output: trivy-results.sarif
        ignore-unfixed: false
        severity: UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL
        limit-severities-for-sarif: true
        exit-code: '0'

    - name: Ladda upp resultatet
      uses: github/codeql-action/upload-sarif@486fec2a3ea2626afcd8c7e9208b4f515078dd7e # codeql-bundle-v2.26.4
      if: always()
      with:
        sarif_file: trivy-results.sarif
        category: ${{ inputs.category }}

    - name: Blockera fixerbara HIGH/CRITICAL-fynd
      uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
      with:
        scan-type: image
        image-ref: ${{ inputs.image-ref }}
        format: table
        ignore-unfixed: true
        severity: HIGH,CRITICAL
        exit-code: '1'
