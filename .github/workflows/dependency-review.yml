name: Dependency review
# Delegates implementation to Avkroken/.github.

on:
  pull_request:
    branches: [main]
  merge_group:
    types: [checks_requested]

permissions:
  contents: read

jobs:
  dependency-review:
    uses: Avkroken/.github/.github/workflows/dependency-review-reusable.yml@814555cc95150b44b64ac2b64f0424c1c0e41260
